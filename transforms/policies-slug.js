/**
 * This transform will update the `policies` content type `slug` value.
 * 
 * @param  {object}   siteData  The Webhook data for a site
 * @param  {Function} callback  The function to call with the updated data
 */
module.exports = function policiesSlug ( siteData, callback ) {

}
